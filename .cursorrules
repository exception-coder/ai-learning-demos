# AI Learning Demos 项目规则

## ⚠️ 核心规则（必须遵守）

### 🎯 规则 1：学习日志自动记录（最高优先级）

**本项目是从 0 到 1 的学习项目，用户的所有疑问都是知识盲区。**

✅ **强制执行：**
- 用户提出技术疑问时，**必须自动**追加到 `/docs/learning-journal.md`
- **无需反问**用户"是否需要记录"
- 只要对话中出现了技术疑问和解答，**一律记录**

📝 **记录内容：**
1. 疑问原文（用户的原始问题）
2. 详细解答（完整的答案和原理说明）
3. 代码示例（相关的代码对比和示例）
4. 关键要点（提炼核心知识点）
5. 常见误区（记录容易混淆的地方）

**文档结构：**
```markdown
### Q: 问题标题

**疑问：**
> 用户的原始问题

**解答：**
详细的解答内容...

**代码示例：**
```java
// 示例代码
```

**关键要点：**
- 要点1
- 要点2

**常见误区：**
- ❌ 错误理解
- ✅ 正确理解
```

**最终目标：**
将学习日志整理成完整的**从 0 到 1 的学习手册**，帮助其他初学者快速上手 Spring AI。

---

### 🎯 规则 2：开发日志记录

每次编码任务必须在 `/docs/dev-log.md` 中追加记录：
- 日期
- 任务描述
- 创建或修改的文件
- 关键设计决策
- 变更原因

---

### 🎯 规则 3：文件归属规则

**所有新增文件必须写入发起需求的当前项目中，禁止写入全局目录。**

| 场景 | ✅ 正确位置 | ❌ 错误位置 |
|------|---------|---------|
| Skill 文件 | `<当前项目>/.cursor/skills/` | `~/.cursor/skills/` |
| 配置文件 | `<当前项目>/` | 用户主目录或其他项目 |
| 文档文件 | `<当前项目>/docs/` | 其他项目的 docs 目录 |
| 代码文件 | `<当前项目>/src/` | 其他项目的 src 目录 |

---

### 🎯 规则 4：技术指南文档规范

**所有技术指南文档必须统一放在 `/docs/guides/` 目录中。**

**禁止在子模块目录（如 llm-demos/）中创建散乱的技术文档。**

**文档命名规范：**
- 使用数字前缀表示阅读顺序：`01-`, `02-`, `03-`...
- 使用小写字母和连字符：`spring-ai-best-practices.md`
- 文件名要清晰表达文档内容

---

## 📋 代码审查清单

在完成任务前，确保：
- [ ] 更新学习日志（`/docs/learning-journal.md`，如有疑问解答）← **最重要**
- [ ] 更新开发日志（`/docs/dev-log.md`，如有代码变更）
- [ ] 新增文件在当前项目内（非全局目录）
- [ ] 使用 Spring AI 而非原生 SDK
- [ ] 使用 ChatClient 而非 ChatModel
- [ ] 使用构造函数注入
- [ ] 遵循 DDD 分层架构

---

## 技术规范（详细内容）

### 1. Spring AI 集成规范

**必须优先使用 Spring AI 框架**进行 AI 相关功能开发，禁止直接调用原生 SDK。

#### 1.1 ChatClient 使用规范

**强制使用 ChatClient 而非 ChatModel**

```java
// ✅ 正确：使用 ChatClient（推荐）
@Service
public class AiService {
    private final ChatClient chatClient;
    
    public AiService(ChatClient.Builder chatClientBuilder) {
        this.chatClient = chatClientBuilder.build();
    }
    
    public String chat(String userMessage) {
        return chatClient.prompt()
            .user(userMessage)
            .call()
            .content();
    }
}
```

#### 1.2 Function Calling 规范

**使用 Spring AI 的 @Tool 注解定义工具**

```java
@Component
public class WeatherTools {
    
    @Tool(description = "获取指定城市的天气信息")
    public WeatherInfo getWeather(
        @ToolParam(description = "城市名称") String city
    ) {
        return new WeatherInfo(city, "晴", 25);
    }
    
    public record WeatherInfo(String city, String condition, int temperature) {}
}

// 在 Service 中注册工具
@Service
public class AgentService {
    private final ChatClient chatClient;
    
    public AgentService(ChatClient.Builder builder, WeatherTools weatherTools) {
        this.chatClient = builder
            .defaultFunctions("getWeather")
            .build();
    }
}
```

#### 1.3 RAG（检索增强生成）规范

```java
@Service
public class RagService {
    private final ChatClient chatClient;
    private final VectorStore vectorStore;
    
    public String chatWithContext(String question) {
        // 1. 检索相关文档
        List<Document> docs = vectorStore.similaritySearch(
            SearchRequest.query(question).withTopK(3)
        );
        
        // 2. 构建上下文
        String context = docs.stream()
            .map(Document::getContent)
            .collect(Collectors.joining("\n"));
        
        // 3. 生成回答
        return chatClient.prompt()
            .system("基于以下上下文回答问题：\n" + context)
            .user(question)
            .call()
            .content();
    }
}
```

---

### 2. 架构规范

#### 2.1 DDD 分层架构

```
src/main/java/com/learning/
├── controller/          # API 层 - 仅处理协议转换
├── application/         # 应用层 - 编排用例
│   ├── service/        # 应用服务
│   └── dto/            # 数据传输对象
├── domain/             # 领域层 - 业务规则
│   ├── model/          # 领域模型
│   ├── service/        # 领域服务
│   └── repository/     # 仓储接口
└── infrastructure/     # 基础设施层
    ├── ai/             # AI 集成（Spring AI）
    ├── persistence/    # 数据持久化
    └── config/         # 配置
```

#### 2.2 依赖注入规范

**强制使用构造函数注入，禁止字段注入**

```java
// ✅ 正确
@Service
public class AiService {
    private final ChatClient chatClient;
    private final VectorStore vectorStore;
    
    public AiService(ChatClient.Builder builder, VectorStore vectorStore) {
        this.chatClient = builder.build();
        this.vectorStore = vectorStore;
    }
}

// ❌ 错误
@Service
public class AiService {
    @Autowired
    private ChatClient chatClient;
}
```

---

### 3. 配置管理规范

```yaml
spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      base-url: https://api.openai.com
      chat:
        options:
          model: gpt-4
          temperature: 0.7
          max-tokens: 2000
    
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: llama2
```

**动态配置刷新：**
- Configuration 类必须添加 `@RefreshScope`
- 避免 `@RefreshScope` Bean 之间的依赖注入
- 使用 `RefreshScope.refreshAll()` 刷新配置

---

### 4. 测试规范

```java
@SpringBootTest
class AiServiceTest {
    
    @Autowired
    private AiService aiService;
    
    @Test
    void testChat() {
        String response = aiService.chat("你好");
        assertThat(response).isNotEmpty();
    }
}
```

---

### 5. 错误处理规范

```java
@Service
public class AiService {
    private final ChatClient chatClient;
    
    public String chat(String message) {
        try {
            return chatClient.prompt()
                .user(message)
                .call()
                .content();
        } catch (Exception e) {
            log.error("AI 调用失败", e);
            throw new AiServiceException("AI 服务暂时不可用", e);
        }
    }
}
```

---

### 6. 日志规范

```java
@Slf4j
@Service
public class AiService {
    
    public String chat(String message) {
        log.info("收到 AI 请求，消息长度：{}", message.length());
        
        long startTime = System.currentTimeMillis();
        String response = chatClient.prompt()
            .user(message)
            .call()
            .content();
        
        long duration = System.currentTimeMillis() - startTime;
        log.info("AI 响应完成，耗时：{}ms，响应长度：{}", duration, response.length());
        
        return response;
    }
}
```

---

## 支持的 AI 模型提供商

- **OpenAI**: `spring-ai-openai-spring-boot-starter`
- **Azure OpenAI**: `spring-ai-azure-openai-spring-boot-starter`
- **Ollama**: `spring-ai-ollama-spring-boot-starter`
- **通义千问**: `spring-ai-alibaba-starter`
- **智谱 AI**: `spring-ai-zhipuai-spring-boot-starter`
